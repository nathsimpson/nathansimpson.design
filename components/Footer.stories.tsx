import { Footer } from './Footer';
import type { Meta } from '@storybook/react';

const meta: Meta = {
  title: 'Footer',
  component: Footer
};

export default meta;

export const Basic = () => <Footer />;
