import { Box } from '@nathsimpson/box';

export const Skeleton = () => {
  return (
    <Box
      bg="emphasis"
      css={{
        width: '100%',
        aspectRatio: '4/3'
      }}
    />
  );
};
